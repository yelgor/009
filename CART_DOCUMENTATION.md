# 🛒 Сторінка Кошика - Документація

## Загальна інформація

Сторінка кошика розроблена для студентів університету, які хочуть позичити обладнання з лабораторії. На сторінці відображаються:
- **Ліворуч**: список обладнання, додане до кошика
- **Праворуч**: форма для оформлення позики обладнання

## Структура файлів

```
src/
├── pages/
│   └── Cart/
│       ├── Cart.jsx              # Головна сторінка кошика
│       └── Cart.module.css       # Стилі для сторінки
├── context/
│   └── CartContext.jsx           # Context для управління кошиком
└── components/
    └── Navbar/
        └── Navbar.jsx            # Обновлена навігація з кнопкою кошика
```

## Компоненти

### 1. Cart.jsx
Головний компонент сторінки кошика, який включає:
- Список товарів у кошику (з можливістю видалення)
- Форму для оформлення позики
- Інформацію про кількість позицій

**Основні функції:**
- `handleRemoveItem(id)` - видалення товару з кошика
- `handleFormChange(e)` - обновлення даних форми
- `handleSubmit(e)` - відправка форми оформлення позики

### 2. CartContext.jsx
React Context для глобального управління станом кошика

**Методи:**
- `addToCart(item)` - додати товар до кошика
- `removeFromCart(id)` - видалити товар з кошика
- `updateQuantity(id, quantity)` - змінити кількість товара
- `clearCart()` - очистити весь кошик

**Hook:**
```javascript
const { cart, addToCart, removeFromCart, updateQuantity, clearCart } = useCart();
```

## Стилізація

Сторінка використовує дизайн-систему, визначену в `src/styles/`:
- **Кольори**: `--accent-green: #b8c98c`, `--cta: #7a74b4`
- **Фон**: `--bg: #f4f1ee`
- **Шрифти**: Inter (sans-serif), Cinzel (serif)

### Адаптивність
- **Desktop** (>1024px): Два стовпців (товари ліворуч, форма праворуч)
- **Tablet** (640px-1024px): Один стовпець з переходом
- **Mobile** (<640px): Оптимізовано для мобільних пристроїв

## Форма оформлення позики

Форма містить три поля:

1. **Корпоративна пошта** (обов'язкова)
   - Тип: email
   - Плейсхолдер: `fito.pn@ucu.edu.ua`

2. **Дата повернення** (обов'язкова)
   - Тип: text
   - Формат: `mm/dd/yyyy`

3. **Коментар** (опціонально)
   - Тип: textarea
   - Плейсхолдер: `Для чого береш обладнання...`

## Як використовувати

### Додати товар до кошика з іншої сторінки

```jsx
import { useCart } from "../../context/CartContext";

export default function Product({ product }) {
  const { addToCart } = useCart();

  const handleAddToCart = () => {
    addToCart({
      id: product.id,
      name: product.name,
      category: product.category,
      quantity: 1,
      image: product.image, // emoji або URL
    });
  };

  return <button onClick={handleAddToCart}>Додати до кошика</button>;
}
```

### Отримати товари з кошика

```jsx
import { useCart } from "../../context/CartContext";

export default function CartSummary() {
  const { cart } = useCart();

  return (
    <div>
      <p>Товарів у кошику: {cart.length}</p>
      {cart.map((item) => (
        <div key={item.id}>{item.name}</div>
      ))}
    </div>
  );
}
```

## Приклад товара

```javascript
{
  id: 1,
  name: "SRM32 Blue Pill",
  category: "Мікроконтролер",
  quantity: 1,
  image: "📟", // emoji або URL до зображення
}
```

## Маршрутизація

Сторінка кошика доступна за маршрутом:
```
/cart
```

Кнопка кошика (🛒) у навігації знаходиться у верхньому правому куті

## Майбутні удосконалення

- [ ] Інтеграція з backend API для збереження кошика
- [ ] Система автентифікації студентів
- [ ] Відправка даних на сервер
- [ ] Сторінка підтвердження позики
- [ ] Email сповіщення
- [ ] История позик користувача
- [ ] Редагування кількості товарів (+ -)
